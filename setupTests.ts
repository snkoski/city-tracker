// src/setupTests.ts
import '@testing-library/jest-dom'; // Adds jest-dom's custom matchers like .toBeInTheDocument()

// You can add other global setup here, for example:
// import { server } from './mocks/server'; // If using MSW for API mocking
// beforeAll(() => server.listen());
// afterEach(() => server.resetHandlers());
// afterAll(() => server.close());
